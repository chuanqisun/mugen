{
  "name": "root",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run dev -w apps/web",
    "build": "npm run build -w apps/web",
    "format": "npm run format -w apps/web"
  },
  "devDependencies": {
    "prettier": "^3.5.3"
  },
  "workspaces": [
    "apps/*"
  ]
}